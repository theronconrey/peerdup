[relay]
host            = "0.0.0.0"
port            = 55002
session_timeout = 300    # seconds to hold an unpaired session before dropping
max_waiting     = 1000   # max concurrent unpaired sessions

[logging]
level = "INFO"   # DEBUG | INFO | WARNING | ERROR
